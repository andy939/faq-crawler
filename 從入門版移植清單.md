# 從公司「入門版」(D:\ai_work\faq_check\webapp) 移植到「進階版」的待辦

這份筆記是在公司那個session整理的，2026-09-09。目的：這個資料夾（進階版，源自家裡做的
爬蟲）功能上已經比公司版扎實很多（修過5個真的會弄壞資料的bug、有 faqlib.py 共用模組、
環境檢查/完整性驗證工具），但公司版有幾個進階版還沒有的功能，列出來供決定要不要補。

## 公司版有、進階版沒有的功能

| 功能 | 說明 |
|---|---|
| 新聞稿爬蟲 | 進階版目前完全是FAQ專用，沒有新聞稿(News.aspx不同分類)抓取功能 |
| AI摘要(Ollama整合) | 進階版沒有AI摘要生成 |
| GitHub Pages公開唯讀查詢頁 | 公司版有一個純前端靜態頁面(把資料庫匯出成JSON跟著發布)，讓任何人開連結就能查詢FAQ、不用下載任何東西、不用架伺服器。進階版沒有這個 |
| 讓同事區網連線 | 進階版網頁工作台寫死只監聽 `127.0.0.1`(app.py:1292)，只有自己電腦能連。公司版有改成 `0.0.0.0` 讓同辦公室的人也能連 |
| 精簡單畫面版面設計 | 公司版做過「不用捲頁、一畫面塞進15列」的畫面優化(collapsible面板+flex版面)，進階版UI可能要重新檢視 |

## 反過來：公司版資料庫有這個真的會壞資料的漏洞，還沒修

公司版 `db.py` 的 `upsert_faq()` 沒有防護「空內容覆蓋既有資料」——跟進階版
HANDOVER.md 提到的 bug #2 是同一類問題。目前公司資料庫**還沒有**因此受害的跡象
（查過36筆抓取失敗的項目，全部是原始批次的PDF/外部連結，沒有後續被覆蓋的跡象），
但這個漏洞還在，只是運氣好沒踩到。**如果之後兩邊要合併/參考同一套邏輯，這個防護
一定要帶著走。**

另外公司版還有一個提過、沒做完的修正：「每日更新」的複查名單排序邏輯，應該從
「照發布日期排序」改成「照上次驗證時間排序」，否則像「112年發布、114年才被改」
這種案例永遠不會被複查到。

## 兩邊資料庫欄位比較

| 欄位 | 公司版(webapp/db.py) | 進階版(faqlib.py/app.py) | 備註 |
|---|:---:|:---:|---|
| 標題/發布機關/發布日期 | ✓ | ✓ | 都有 |
| 資料更新/資料檢視/資料維護 | ✓ | ✓ | 都有 |
| **下版日期(expire)** | ✗ | ✓ | 公司版完全沒抓，這是FAQ預定自動下架的日期，對維運「哪些快過期」很有用 |
| 內容 | ✓ 純文字(截斷5000字) | ✓ 純文字 + **原始HTML格式(answer_html)** | 進階版多存一份含格式(超連結/換行/粗體)的版本 |
| 附件資訊 | ✓ 只有(名稱、連結) | ✓ (名稱、**類型**、**大小**、連結) | 進階版資訊更完整 |
| **是否為外部連結(kind)** | ✗ 用內容字串判斷失敗 | ✓ 明確標記internal/external | |
| **最後出現在清單的時間(last_listed)** | ✗ | ✓ | 可用來偵測「已經從官網下架」的舊項目，公司版沒有這個追蹤機制 |
| 點閱數(view_count) | ✓ | ✗ | 這個反而是公司版多的 |
| AI摘要 | ✓ | ✗ | 進階版沒有 |

## 建議優先順序（給接手的session參考，不是定案）

1. 先確認/補上「空內容不覆蓋既有資料」的防護（如果進階版繼續發展，這個原則要貫徹到底）
2. 下版日期、外部連結標記、清單消失偵測（last_listed）——這幾個對實際維運FAQ資料庫價值較高
3. GitHub Pages公開查詢頁、同事區網連線——如果還是想要「多人查詢」這個目標的話
4. 新聞稿爬蟲、AI摘要——看之後要不要繼續做新聞稿這條線
