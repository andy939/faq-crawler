# 搬到公司 Win10

## 一句話

**要帶的資料夾是 `~/Documents/faq-crawler/`**（就是這個資料夾）。
其他 `爬蟲`、`爬蟲_完整_20260909`、`爬蟲_公司包_20260909`、`1150909 FAQ爬蟲`
都是舊的分岔版本，已經合併進來了，**不要帶**。

---

## 為什麼是這個資料夾

2026-09-12 清點時發現有四個資料夾、686 MB，內容互相不一樣：

| 資料夾 | 筆數 | 最後抓取 | 程式碼 |
|---|---:|---|---|
| `爬蟲/` | 8,312 | 09/11 00:01 | **舊**（app.py 1294 行） |
| `爬蟲_完整_20260909/爬蟲/` | 8,304 | **09/11 15:49** | **新**（1470 行） |
| `爬蟲_公司包_20260909/爬蟲/` | 8,308 | 09/09 00:46 | 新 |
| `1150909 FAQ爬蟲/` | — | — | 上面兩包的再複製 |

三個維度指向三個不同資料夾，所以不能直接複製任何一個。

**合併結果**：以 `爬蟲_完整/`（程式與資料都最新）為基準，
把 `爬蟲/` 獨有的 8 筆有內文的問答撈進來 → **8,312 筆，有內容 8,272 筆**。

---

## 怎麼搬

### 方法 A：GitHub（建議，之後兩邊同步都靠它）

程式已經是 git repo，只差推上去。

**這台 Mac：**

1. 裝 [GitHub Desktop](https://desktop.github.com)，登入
2. `File → Add Local Repository` → 選 `~/Documents/faq-crawler`
3. 按 `Publish repository`，**勾選 Keep this code private**

**公司 Win10：**

1. 裝 GitHub Desktop，登入同一個帳號
2. `Clone repository` → 選剛才那個 repo → 存到例如 `D:\faq-crawler`
3. 雙擊 `啟動.bat`

之後改了東西：一邊 Commit + Push，另一邊 Pull。不用再搬 zip。

### 方法 B：直接複製（只想先跑起來）

把整個 `faq-crawler` 資料夾複製到隨身碟 → 公司電腦，
放在例如 `D:\faq-crawler`，雙擊 `啟動.bat`。

缺點是之後兩邊又會分岔，就是這次的問題。

---

## 資料庫要另外傳

**`faq.db` 沒有進 git**，因為它 61 MB 且每次抓取都會變，
放進版本控制的話 repo 每天肥 60 MB，一個月就 1.8 GB。

```
  進 git 的：程式 + 文件      約 208 KB
  不進 git：faq.db、exports/、xlsx、備份
```

所以用方法 A 的話，clone 下來**不會有資料**，要另外把 `faq.db` 複製過去
（放在資料夾根目錄，跟 `app.py` 同一層）。

傳輸方式建議 **Google Drive 或 iCloud**：家裡跑完上傳，公司下載覆蓋。
方向是單向的 —— **只有家 → 公司，不要反向**，雙向同步資料庫一定會衝突。

---

## 公司電腦第一次

1. 雙擊 **`啟動.bat`**
   （會自動找 Python、缺套件自己裝、開瀏覽器到 http://127.0.0.1:8765）
2. 網頁上按 **「環境檢查」**

環境檢查會告訴你八件事，每項不過都附解法：

| 檢查 | 公司常見問題 |
|---|---|
| Python 版本 | 需要 3.9+，沒有的話去 python.org 裝 |
| 套件 | `requests` 必要，`openpyxl` 只影響 Excel |
| Proxy | 自動讀 `HTTPS_PROXY` |
| **TLS 憑證** | **公司做 TLS 攔檢會失敗。跟資訊單位要根憑證，設 `REQUESTS_CA_BUNDLE`。不要改成 `verify=False`** |
| 對外 IP | 公司通常整棟共用一個，WAF 按 IP 算 |
| 清單解析 | 站方改版的話這裡會先發現 |
| **WAF 探測** | **現在會不會被擋，決定要不要跑** |
| 資料庫 | 筆數、最後抓取時間 |

---

## 平常怎麼用

**只需要記一件事：雙擊 `啟動.bat`。** 其他全部在網頁上按按鈕。

| 網頁上的按鈕 | 什麼時候用 |
|---|---|
| **每日更新** | 平常用這個。只抓新增的，十幾次請求、幾秒完成 |
| **環境檢查** | 換電腦、換網路、或覺得怪怪的時候 |
| **完整性驗證** | 想確認內文沒被截斷（重抓最長 25 筆逐字比對） |
| 開始抓取 | 抓整站。**不建議在公司跑**，見下 |
| 匯出 Excel / CSV / JSON | 下方資料區，可先用日期或關鍵字篩選 |

### 不要在公司跑整站

9/8 實測：辦公室白天跑會被擋。很可能是整棟共用一個對外 IP，
WAF 按 IP 計算，你的爬蟲流量跟同事的瀏覽算在一起。

**整站在家晚上跑（約 50 分鐘），把 `faq.db` 帶過去就好。**
公司只跑「每日更新」，那個量 WAF 不會有反應。

---

## 兩套程式還沒合併

公司還有一套「入門版」在 `D:\ai_work\faq_check\webapp`，
跟這套各有對方沒有的功能。詳見 `從入門版移植清單.md`。

| | 公司入門版 | 這套 |
|---|:---:|:---:|
| 新聞稿爬蟲 | ✓ | ✗ |
| AI 摘要（Ollama） | ✓ | ✗ |
| GitHub Pages 公開查詢頁 | ✓ | ✗ |
| 讓同事區網連線 | ✓ | ✗（寫死 127.0.0.1） |
| 點閱數 | ✓ | ✗ |
| 下版日期 | ✗ | ✓ |
| 附件類型／大小 | 部分 | ✓ |
| `last_listed` 下架偵測 | ✗ | ✓ |
| **空內容不覆蓋的防護** | **✗ 漏洞還在** | ✓ |
| `faqlib.py` 共用模組 | ✗ | ✓ |

**建議最後只留一套。** 維護兩套正是這次「四個資料夾三個版本」的根源。

如果要繼續用公司入門版，**至少先補上「空內容不覆蓋」的防護**——
那個 bug 在這邊曾經把 5,947 筆好資料覆蓋成空的。

---

## 舊資料夾怎麼處理

合併完成後，Mac 上這些可以刪：

```
  ~/Documents/爬蟲/                    210 MB
  ~/Documents/爬蟲_完整_20260909/      196 MB
  ~/Documents/爬蟲_公司包_20260909/     42 MB
  ~/Documents/1150909 FAQ爬蟲/         238 MB
```

**刪之前先確認 `faq-crawler` 跑得起來、資料筆數正確**（8,312 筆）。
`~/Documents/` 底下還有 `爬蟲_公司包_20260909.zip` 等壓縮檔，留著當存檔沒關係。
