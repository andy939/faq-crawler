# 進度交接 — 2026-09-09

家中 Mac 做的，要搬到公司 Win10。**技術細節看 `README.md`，這份講進度、坑、和怎麼開始。**

---

## 一句話

全站約 8,300 筆常見問答已抓完存進 `faq.db`，工具齊備。
兩天下來最大的收穫不是爬蟲本身，是**發現並修掉五個會弄壞資料的 bug**——
其中兩個曾經讓資料靜靜地少掉或被覆蓋，而且不會報錯。

---

## 到新電腦怎麼開始

1. 複製整個資料夾
2. 雙擊 **`0_安裝套件.bat`**
3. 雙擊 **`1_環境檢查.bat`** ← **一定要先跑這個**

`check_env.py` 會檢查八項並告訴你每項不過該怎麼辦：

| 檢查 | 公司常見的問題 |
|---|---|
| Python 版本 | 需要 3.9+ |
| 套件 | `requests` 必要，`openpyxl` 只影響 Excel 匯出 |
| Proxy | 會自動讀 `HTTPS_PROXY` |
| **TLS 憑證** | **公司做 TLS 攔檢會失敗。跟資訊單位要根憑證，設 `REQUESTS_CA_BUNDLE`。絕對不要改成 `verify=False`** |
| 對外 IP | 公司通常整棟共用一個，WAF 是按 IP 算的 |
| 清單解析 | 站方改版的話這裡會先發現 |
| **WAF 探測** | **告訴你現在會不會被擋，決定要不要跑** |
| 資料庫 | 筆數、最後抓取時間 |

日常就這三個：

```
  2_每日更新.bat      約 3~10 次請求，幾秒完成  ← 平常用這個
  3_網頁工作台.bat    要查詢、篩選、匯出時
  5_驗證完整性.bat    想確認內容沒被截斷時
```

**不建議在公司跑全站**（`4_抓整站.bat`）。9/8 實測辦公室白天會被擋。
全站在家晚上跑，把 `faq.db` 帶過去就好。

---

## 檔案

| 檔案 | 用途 |
|---|---|
| **`faqlib.py`** | **共用模組。站台常數、連線、解析全在這裡。改 bug 改這一個就好。** |
| `app.py` | 本機網頁工作台 → http://127.0.0.1:8765 |
| `update.py` | 每日增量更新（含限流探測閘門） |
| `crawl_faq.py` | 命令列整站版，可中斷續傳 |
| `verify.py` | 內容完整性驗證 |
| `check_env.py` | 環境檢查 |
| `report.py` / `to_excel.py` / `bench.py` | 分析報表、Excel、速度測試 |
| `0~5_*.bat` | Windows 批次檔 |
| `faq.db` | SQLite。`faq` 資料、`samples` 逐筆測量、`runs` 執行紀錄 |
| `exports/` | 每次執行自動存的逐筆明細 CSV |

**為什麼有 `faqlib.py`**：原本解析邏輯在六個檔案各複製一份，
9/8 同一個 bug 修了三次還漏一個。抽成共用模組後全站只有一份。

---

## 資料

- 站上約 **8,309 筆**（含 39 筆連到外部網站、沒有站內內文的）
- 日期 **2007-01-01 ~ 今**，233 個發布機關
- 欄位：**發布時間、更新時間、檢視時間、下版日期、標題、發布機關、內容、附件**
  - `下版日期` = 預定自動下架的日期，維護時最有用
  - `檢視時間` = 站方最後一次檢視
  - `附件` = 檔名、類型、大小、下載網址

**內容空白不代表沒內容** —— 有些問答的答案本身就是一份 PDF。
統計「有內容」要算 `answer <> '' OR file_count > 0`。

---

## 五個弄壞資料的 bug（都已修）

### 1　被擋時伺服器回「錯誤頁」而不是錯誤碼

被擋時站方回一個 **3 KB 的錯誤頁，HTTP 狀態碼仍然是 200**。
程式只看狀態碼就把它當正常頁解析（解出空字串）寫入，
**把 5,947 筆好內容覆蓋成空的**。

正常內文頁約 197 KB，錯誤頁 3,237 bytes。
**修法**：`faqlib.is_blocked()` —— 狀態碼不對、頁面 < 50 KB、
或找不到內文區塊，一律當作沒抓到。

### 2　空內容覆蓋既有資料

upsert 沒防護。**修法**：`WHERE excluded.answer <> '' OR excluded.file_count > 0`。

### 3　清單分頁沒去重

站上清單是活的，同一筆會出現在相鄰兩頁。一輪多抓 109 次，筆數也灌水。
**修法**：依 `sid` 去重。

### 4　清單分頁的無窮迴圈

站方對超出範圍的頁碼**不回空值，而是一直回最後一頁**
（page=43、44、60 都回同樣 109 筆）。加去重後 `items` 不再成長，
而迴圈條件是 `len(items) < n`，於是無限翻頁。白打 353 次請求。

**修法**：改用「整頁都是看過的」當結束訊號。
更好的作法是先讀站上印的總筆數算頁數 —— `crawl_faq.py` 本來就這樣做，
`app.py` 沒有。這是抽 `faqlib.total_count()` 的原因之一。

### 5　純附件的問答存不進去

有些問答內文是空的，答案是一份 PDF。而 bug 2 的修正（空內容不覆蓋）
會把這類問答整個擋掉。

**修法**：抓「相關檔案」區塊（`faqlib.parse_files`），
寫入條件改成「有內文**或**有附件」。

### 這些 bug 讓我先前的分析全錯

9/7 晚上那輪 B，我報告「中間 11 段完全乾淨（52~86ms）」——
**那 11 段 100% 是錯誤頁**，快是因為錯誤頁只有 3 KB。
8,224 次請求裡只有 2,282 次拿到真內容。

**教訓：收到回應要先確認它長得像不像目標頁面，再談效能分析。**

---

## 限流：觀察到的現象（不是結論）

會加罰約 2 秒 TTFB，或直接回錯誤頁。實測過的時段：

| 時間 | 抓法 | 筆數 | 結果 |
|---|---|---:|---|
| 9/6 08:44~09:15 | A~F | 99 / 500 | 乾淨 |
| 9/6 13:28~18:20 | B、E | 1,984 / 8,277 | 挨罰 |
| 9/6 20:09、21:40 | B、E | 8,277 | 乾淨（50 分 / 47 分） |
| 9/7 21:05 | B | 8,224 | 大量錯誤頁 |
| 9/8 白天（辦公室） | B、E | — | 被擋 |
| 9/8 22:50 | E | 8,231 | 順利（46.8 分，被擋 11 次） |

**我提過四個假說，全部被自己的資料推翻**：不重用連線才會被罰、
約 500 筆觸發、3 條連線會觸發、晚上就安全。**現在不再提新假說。**

**能確定的**：它會來會走、會自行解除、白天和辦公室網路特別容易中。

**實務**：在家、晚上、限速 333ms 跑全站約 50 分鐘。
辦公室只跑 `update.py`。

---

## 效能實測（9/6，6 方法 × 500 筆 × 3 輪）

| 方法 | 每筆 | 等伺服器 | 傳輸 |
|---|---:|---:|---:|
| A 每筆重開連線 | 218ms | 391ms | 86ms |
| **B 直接抓・循序** | **128ms** | 47ms | 40ms |
| C 關掉 gzip | 297ms | 50ms | **226ms** |
| D 完整瀏覽器流程 | 139ms | 77ms | 37ms |
| **E 併發 3** | **44ms** | 88ms | 99ms |
| F 併發 8 | 43ms | **261ms** | 109ms |

- **gzip 影響最大**：關掉慢 2.3 倍、流量 3.6 倍。省的是傳輸時間，不是伺服器時間。
- **連線重用次之**：每筆重開 TLS 多花約 125ms（TCP 24ms + TLS 25ms + 其他）。
- **「帶 cookie 那些」不貴**：方法 D 多打一倍請求也才慢 9%。
- **限速之後併發沒意義**：加了 333ms 限速，B 50 分 / E 47 分，
  限速器才是瓶頸。只有挨罰時多開連線才能攤掉那 2 秒。
- **解析時間 0.4~0.6 ms/筆**，完全可以忽略。瓶頸不在 Python。

### 位元效率

| | |
|---|---|
| 一頁 HTML | 約 197 KB |
| gzip 後下載 | 約 55 KB（28%） |
| 真正需要的欄位 | 中位 **521 B**（最小 88、最大 22,043） |
| **有效率** | **約 1/100** |

固定成本迴歸：`抓下 = 53,997 + 1.52 × 需要`（r²=0.973）。
**每頁 54 KB 是版型骨架，跟內容一個字都沒關係。**
所以抓取時間跟內容長短幾乎無關（r² < 0.03）。

---

## 之後可以做的

1. **下版日期維護檢視**：哪些快到期、哪些已過期還掛著、哪些沒設、
   哪些超過一年沒檢視。欄位都有了，做個查詢頁就行。
2. **全文檢索**：SQLite FTS5，做成真正可查詢的問答站（原本的目標）。
3. **排程**：每晚自動跑 + 產出「今天哪幾筆被改了」的異動清單。
4. **自適應限速**：偵測 TTFB 變慢就自動放慢。
5. **改用 BeautifulSoup**：現在用 regex 解析（零安裝），但 bug 3、4 都跟
   regex 有關。穩定後值得換。

### 做實驗前務必

**先跑 `1_環境檢查.bat` 或 `python verify.py`，確認起跑狀態乾淨。**
9/8 有一輪 E 就是因為一開始就在挨罰，25 分鐘的資料完全不能用。

---

## 必記的坑

**1　Python 連這站會 SSL 錯誤**

```
CERTIFICATE_VERIFY_FAILED: Missing Subject Key Identifier
```

不是憑證有問題，是 OpenSSL 3.x 的 RFC5280 嚴格模式。
`faqlib.ssl_context()` 已處理（`verify_flags &= ~ssl.VERIFY_X509_STRICT`）。
**不要用 `verify=False`**，那是真的把中間人防護關掉。

**2　清單解析不能用跨列的 regex**

清單混著外部連結，跨 `<tr>` 的 `.*?` 會把後面那筆一起吃掉。
第一版少了 40 筆，**不會報錯，只會靜靜少資料**。要先切 `<tr>` 再逐列解析。

**3　被擋時是 HTTP 200 + 3 KB 錯誤頁**

不能只看狀態碼。用 `faqlib.is_blocked()`。

**4　站方對超出範圍的頁碼一直回最後一頁**

不能靠「翻到沒東西」判斷結束，會無窮迴圈。

**5　內文空白可能是因為答案是 PDF 附件**

不是抓失敗。要抓 `div.group-list.file-download-multiple`。

**6　Windows 主控台是 cp950，印繁體中文會爆**

`faqlib.py` 開頭已處理，批次檔也加了 `chcp 65001`。

**7　openpyxl 不收控制字元**

內文偶爾混到 `\x00-\x1f`，匯出 Excel 會炸。要先過濾。

**8　民國年字串不能直接排序**

`96-01-01` 和 `115-09-08` 字典序會顛倒。用 `faqlib.roc_to_iso()` 轉西元。

**9　SQLite 連線不能跨執行緒**

寫入執行緒要自己開一條。

---

## 網站結構（省得再摸）

純 server-render 的 ASP.NET CCMS，不需要 JS、不需要 cookie。

**清單**：`/News.aspx?n=EEC70A4186D4C828&sms=87415A8B9CE81B16&page=N&PageSize=200`
總筆數在 `<span class="count"><i>/</i>8309</span>`。

**內文**：`/News_Content.aspx?n=...&sms=...&s=<16碼HEX>`
內容在 `div.area-essay.page-caption-p` 的 `div.essay > div.p`，
中繼資料是一串 `<span>標籤：值</span>`。

**日期查詢只能 POST**，GET 參數完全被忽略。action 要帶 `&Create=1`，
送 `__VIEWSTATE` + `jNewsModule_field_SDate_1/EDate_1`（民國 114/01/01 格式）。
回應給一個 `_Query=<GUID>`，**那個 token 是無狀態的**，之後翻頁可平行。
