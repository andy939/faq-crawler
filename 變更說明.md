# 變更說明 — 2026-09-12 改成 GitHub 版

這份是**變更帳**。目的只有一個：讓你不用去猜我動了什麼。
每一個被刪掉的檔案、每一個被拿掉的欄位，都在這裡有對應。

技術細節看 `README.md`，這份只講「原本有什麼、現在在哪」。

---

## 一句話

資料從 SQLite（`faq.db`，61 MB、不能進 git）改成 `docs/faq.json`（跟著 git 走），
所以兩台電腦和 GitHub Actions 看到的是同一份，不用再搬資料庫。
網頁從只能在本機跑的 `app.py`，改成一頁兩用：
放 GitHub Pages 上是唯讀監控，在本機打開就長出完整的控制台。

---

## 檔案：原本的去哪了

| 原本的檔案 | 現在 |
|---|---|
| `app.py`（1,655 行） | **併進 `crawl.py` + `工作台.py` + `docs/index.html`** |
| `crawl_faq.py` | 併進 `crawl.py`（`--full`） |
| `update.py` | 併進 `crawl.py`（預設的「補齊」） |
| `check_env.py` | 併進 `crawl.py`（`--check`） |
| `verify.py` | 併進 `crawl.py`（每次跑完都會對帳，不用另外驗） |
| `bench.py` | **刪掉**。改成 `crawl.py --method X --limit N`，同樣的事 |
| `report.py` | **還在**，改成讀 `docs/runs.csv` + `exports/*.csv` |
| `to_excel.py` | **還在**，改成讀 `docs/faq.json`，多了維護建議和機關統計 |
| `HANDOVER.md` | 併進 `README.md`（那 10 個坑一條都沒少） |
| `搬到公司電腦.md` | 併進 `README.md`（現在靠 git 同步，不用搬） |
| `從入門版移植清單.md` | 刪掉（已經移植完了） |
| `report_500.md` `report_全站.md` | 刪掉，結論寫進 `README.md`。**原始測量資料還在 `faq.db`** |
| `faq.db` | **沒刪**，留在本機當備份。程式已經不用它 |

**所有被刪的檔案都在 git 歷史裡**，要看舊版：

```bash
git show 748bf2d^:app.py > app_old.py
```

---

## 欄位：資料少了什麼

舊的 `faq` 資料表有 22 欄，新的 `docs/faq.json` 有 13 個。差額：

| 舊欄位 | 現在在哪 | 為什麼 |
|---|---|---|
| `answer_html` | **`docs/faq_html.json`** | 一度被我刪掉，9/12 已從 `faq.db` 救回 8,257 筆。分開存是因為它有 369 萬字，網頁只需要純文字的 181 萬字 |
| `bytes_wire` `bytes_raw` `bytes_needed` | `exports/*.csv` | 那是「這次抓取的量測」，不是「這筆問答的屬性」。放在逐筆測量檔裡才對 |
| `no`（清單序號） | 不存 | 官網每新增一筆，全部的序號都位移。存了的話每次 commit 都是八千行全改 |
| `fetched_at` `last_listed` | 不存 | 同上，每次抓都變。複查改用 `meta.json` 的游標輪流 |
| `date` `published_iso` | 合併成 `published` | 三個欄位講同一件事。現在統一存西元 |
| `file_count` | 不存 | 等於 `len(files)`，算得出來 |
| — | **新增 `gone`** | 確認下架的日期。資料不刪，只是不列入計數 |

舊的 `samples` 表（12.5 萬列逐筆測量）**留在 `faq.db` 裡沒動**，
新的逐筆測量寫到 `exports/`，格式幾乎一樣。

---

## 行為：哪些不一樣了

**1　「官網幾筆就抓幾筆」變成每次都做**

以前 `update.py` 只翻清單前 5 頁看有沒有新的。現在每次都抓完整清單
（42 頁、約 48 次請求、十幾秒）跟手上的資料對帳，最後印
「站上 N 筆　已下載 N 筆　★ 一致」。

**2　下架要逐筆確認才算**

清單是活的，抓那兩分半裡順序會位移，有筆從第 34 頁擠到第 33 頁就會被漏看。
所以「不在清單裡」的會再去敲一次它的內文頁：開得起來就是還在，
開不起來才標下架。9/12 用這個方法確認了 9 筆真的下架。

**3　外部連結的 key 改成「網址｜標題」**

站上有兩筆不同的問答（殯葬處那兩筆）連到同一個外部網址，
原本只用網址當 key 會把它們併成一筆，筆數就永遠比官網少一。

**4　逐筆跟前一版比對**

總筆數一樣不代表沒事 —— 可能今天新增一筆、同時下架一筆。
所以新增／異動／下架／復原都記進 `docs/changes.csv`，
異動還會註明是哪個欄位變的（例如「內容(+312字)」）。

**5　排程平日那班會複查**

官網清單只印發布日期、不印更新時間，所以既有問答的內容被改了，
光靠「補齊」看不出來。平日那班改成「補齊＋複查 500 筆」，
約 17 天輪完全站；週日的全站重抓保證最慢 7 天一定發現。

**6　限流門檻在 GitHub 上放寬**

探測中位數超過門檻就跳過本次。本機是 800ms，GitHub Actions 在美國，
跨太平洋來回就 700ms 起跳 —— 9/12 第一次在 GitHub 上跑是 755ms，
離誤判只差 45ms。所以在 GitHub 上放寬到 1800ms（挨罰是 +2000ms 以上）。

---

## 沒有變的

- `faqlib.py` **一個字都沒改**。站台常數、HTML 解析、擋錯誤頁、限速器都還是它
- 那 10 個坑的處理方式全部保留（錯誤頁判斷、空內容不覆蓋、分頁去重、
  總筆數算頁數、民國年轉西元、SSL 旗標、cp950…）
- 抓法 A～F 六種全在，量測的東西一項沒少
- 抓取的實際行為：一樣走 `crawl.py`，不管你是雙擊 `更新.bat`、
  在網頁上按「開始抓取」、還是在 GitHub 上按按鈕

---

## 驗證紀錄（2026-09-12）

| 項目 | 結果 |
|---|---|
| 官網 vs 已下載 | 8,304 / 8,304 ★ 一致 |
| 原始 HTML 救回 | 8,257 筆（369 萬字） |
| GitHub Actions 實跑 | 成功，24.3 秒，48 次請求，被擋 0 次 |
| GitHub 的美國 IP 會不會被擋 | **不會**（這是先前唯一的未知數） |
| 本機控制台 | 八個指標即時更新、進度條、停止鍵都正常 |
| 停止鍵 | 是「好好地停」：已抓到的存檔後才結束 |
| Excel 匯出 | 9 個工作表、4.0 MB |
| 分析報表 | `report.md` |
